name: m45_app
description: M45 - Aplicativo de Mobilização Política
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  pocketbase: ^0.18.0
  hive: ^2.2.0
  hive_flutter: ^1.1.0
  path_provider: ^2.1.0
  provider: ^6.1.0
  get_it: ^7.6.0
  shared_preferences: ^2.2.0
  url_launcher: ^6.2.0
  image_picker: ^1.0.0
  http: ^1.1.0
  connectivity_plus: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.0
  build_runner: ^2.4.0
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
